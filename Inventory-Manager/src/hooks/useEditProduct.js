import { useState } from "react";

export const useEditProduct = (url,id) => {
      const [isEditCreating, setIsEditCreateting] = useState(false);
    
      if(!id) return
    async  function editProduct(){
        try {
            
        } catch (error) {
            console.log(error)
        }
      }
    
    return {}
}